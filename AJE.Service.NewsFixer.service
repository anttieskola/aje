# /etc/systemd/system/AJE.Service.NewsFixer.service

# install
# sudo systemctl daemon-reload
# sudo systemctl enable AJE.Service.NewsFixer.service
# sudo systemctl start AJE.Service.NewsFixer.service

# removal
# sudo systemctl stop AJE.Service.NewsFixer.service
# sudo systemctl disable AJE.Service.NewsFixer.service
# remove the service file
# reload daemon

# full log you can see with
# journalctl -u AJE.Service.NewsFixer

[Unit]
Description=AJE.Service.NewsFixer
After=network.target

[Service]
Type=simple
Restart=always
# Note working directory is a must
# otherwiser process can't find resources -> odd errors
WorkingDirectory=/usr/local/bin/AJE.Service.NewsFixer
ExecStart=/usr/local/bin/AJE.Service.NewsFixer/AJE.Service.NewsFixer
User=antti

SyslogIdentifier=AJE.Service.NewsFixer
StandardOutput=syslog
StandardError=syslog

[Install]
WantedBy=multi-user.target
